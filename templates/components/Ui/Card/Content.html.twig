{# @block content The default block #}
<div
    class="{{ 'p-6 pt-0 mt-10 sm:mx-auto sm:w-full sm:max-w-sm lg:max-w-lg ' ~ attributes.render('class')|tailwind_merge }}"
    {{ attributes }}
>
  {%- block content %}{% endblock -%}
</div>
