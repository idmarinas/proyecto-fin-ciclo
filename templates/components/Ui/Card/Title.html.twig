{# @block content The default block #}
<div
    class="{{ 'text-2xl/9 font-semibold leading-none tracking-tight text-center' ~ attributes.render
      ('class')|tailwind_merge }}"
    {{ attributes }}
>
  {%- block content %}{% endblock -%}
</div>
