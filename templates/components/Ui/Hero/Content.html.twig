{# @block content The default block #}
<div class="{{ ('flex flex-col space-y-8 w-full ' ~ attributes.render('class'))|tailwind_merge }}" {{ attributes }}>
  {%- block content %}{% endblock -%}
</div>
