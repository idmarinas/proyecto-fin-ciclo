{# @block content The default block #}
<div
    class="{{ ('flex items-center justify-center gap-x-6 ' ~ attributes.render('class'))|tailwind_merge }}"
    {{ attributes }}
>
  {%- block content %}{% endblock -%}
</div>
