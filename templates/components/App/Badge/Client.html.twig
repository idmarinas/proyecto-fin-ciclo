{% props is_client %}

{% set text = true === is_client ? 'Cliente' : 'Usuario' %}

{% set class = 'px-3 py-1 text-xs rounded ' ~ html_classes({
  'bg-green-100 text-green-700': 'Cliente' === text,
  'bg-gray-200 text-gray-700': 'Usuario' === text
}) ~ ' ' ~ attributes.render('class') %}

{% dump(text) %}
{% dump(class) %}

{% component 'Ui:Badge' with {...attributes, class} %}
  {% block content text %}
{% endcomponent %}
