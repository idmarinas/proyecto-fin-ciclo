<header
    class="{{ ('sticky top-0 border-b-2 border-b-gray-300 backdrop-blur-xs z-50 ' ~ attributes.render('class'))
    |tailwind_merge }}"
    {{ attributes }}
>
  <nav aria-label="Global" class="mx-auto flex max-w-7xl items-center justify-center lg:justify-between p-6 lg:px-8">
    <div class="flex lg:flex-1">
      <a href="{{ path('app_home') }}" class="hover:bg-black/20 dark:hover:bg-white/20 rounded-sm">
        <span class="sr-only">{{ app_title }}</span>
        <img src="{{ asset('images/public_thread.webp') }}" alt="" class="h-10 w-auto" />
      </a>
    </div>
    <div class="hidden lg:flex lg:flex-1 lg:justify-end">
      {% if app.user is empty %}
        <twig:App:Nav:Anonymous />
      {% else %}
        <twig:App:Nav:Profile />
      {% endif %}
    </div>
  </nav>
</header>
