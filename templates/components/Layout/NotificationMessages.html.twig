{% props forced = false %}

<twig:Turbo:Frame id="container-notification-messages">
  <twig:NotificationMessages messages="{{ app.request.preferredFormat != 'turbo_stream' or forced ? app.notifications : [] }}" />
</twig:Turbo:Frame>
